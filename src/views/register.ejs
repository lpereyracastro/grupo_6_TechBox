<!DOCTYPE html>
<html lang="en">

<%- include('./partials/head') %>

<body>
    <!-- HISTORIA DE USUARIO -->
    <!--
        Como cliente quiero poder registrarme para estar en su base de datos y acceder a mi perfil e historial de compras de una manera más sencilla y, además, facilitar el proceso de futuras compras.
    -->
    <%- include('./partials/header') %>
    <main>
        <article class="form-login-register">
            <form action="/user/userRegister" method="POST" class="form" enctype="multipart/form-data">
                <h4>Crear una cuenta de</h4>
                <h2>TechBox</h2>
                <div class="form__campos">
                    <input type="user" placeholder="User" name="firstname" value="<%= locals.oldData ? oldData.firstname : null %>">
                    <% if (locals.errors && errors.firstname) { %>
                        <p class="form__error">
                            <%= errors.firstname.msg %>
                        </p>
                    <% } %>
                    <input type="email" name="email" placeholder="Direccion de correo electronico" value="<%= locals.oldData ? oldData.email : null %>">
                    <% if (locals.errors && errors.email) { %>
                        <p class="form__error">
                            <%= errors.email.msg %>
                        </p>
                    <% } %>
                    <input type="password" name="password" placeholder="Password" value="<%= locals.oldData ? oldData.password : null %>">
                    <% if (locals.errors && errors.password) { %>
                        <p class="form__error">
                            <%= errors.password.msg %>
                        </p>
                    <% } %>
                    <input type="file" name="image" value="<%= locals.oldData ? oldData.image : null %>">
                </div>
                <div class="form__buttom">
                    <input type="submit" value="Registrarse">
                </div>
                <div class="form__register-login">
                    <p>¿tienes cueta?</p>
                    <a href="/user/userLogin" class="form__link">Inicia sesion!</a>
                </div>
            </form>
        </article>
    </main>
    <%- include('./partials/footer') %>
    <script src="/js/script.js"></script>
</body>

</html>