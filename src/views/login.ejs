<!DOCTYPE html>
<html lang="en">

<%- include('./partials/head') %>

<body>
    <!-- HISTORIA DE USUARIO -->
    <!--
        Como cliente quiero poder loguearme para acceder a mi historial de compras.
        
        Como cliente quiero tener la opción de que el sitio me recuerde para no escribir mi usuario y contraseña cada vez.

        Como cliente quiero poder recuperar la contraseña para poder ingresar si me la olvido.
    -->
   <%- include('./partials/header') %>
    <main>
        <article class="form-login-register">
            <form action="/user/userLogin" method="POST" class="form">
                <h4>Inicia sesión con una cuenta de</h4>
                <h2>TechBox</h2>
                <% if (locals.messageAuth) { %>
                    <p style="color: red;">
                        <%= messageAuth %>
                    </p>
                <% } %>
                <div class="form__campos">
                    <input type="email" name="mail" placeholder="Direccion de correo electronico" autocomplete="on">
                    
                    <% if (locals.emailAuth) { %>
                        <p class="form__error">        
                            <%= emailAuth %>
                        </p>
                    <% } else if (locals.errors && errors.mail) { %>
                        <p class="form__error">        
                            <%= errors.mail.msg %>
                        </p>
                    <% } %>


                    <input type="password" name="password" placeholder="password" autocomplete="off">
                    <% if (locals.passwordAuth) { %>
                        <p class="form__error">        
                            <%= passwordAuth %>
                        </p>
                    <% } else if (locals.errors && errors.password) { %>
                        <p class="form__error">
                            <%= errors.password.msg %>
                        </p>
                    <% } %>
                </div>
                <div class="form__recuerdame">
                    <div class="form__recuerdame-campos">
                        <label for="recuerdame">
                            <input type="checkbox" name="recuerdame" id="recuerdame">
                            Recuerdame
                    </label>
                    </div>
                    <a href="/cambiarContraseña" class="form__link">¿Has olvidado tu contraseña?</a>
                </div>
                <div class="form__buttom">
                    <input type="submit" value="Iniciar"></input>
                </div>
                <div class="form__register-login">
                    <p>¿No tienes cuenta?</p>
                    <a href="/user/userRegister" class="form__link">registrate !</a>
                </div>
            </form>
        </article>
    </main>
    <%- include('./partials/footer') %>
    <script src="/js/script.js"></script>
</body>

</html>